# sysctl

Role configure sysctl

## Usage

Overwrite `sysctl_parameters` and role install this version

## Variables

| Name | type | default | description |
|------|------|---------|-------------|
| `sysctl_parameters` | list | | List of string in `sysctl -w` format |
